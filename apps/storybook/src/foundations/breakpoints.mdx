import { Meta } from "@storybook/addon-docs/blocks";
import { BreakpointBars } from "./blocks/scales";

<Meta title="Foundations/Breakpoints" />

# Breakpoints

Mobile-first: style mặc định áp dụng cho mọi kích thước, breakpoint là **min-width**.

<BreakpointBars />

## Rule

- Breakpoint phải đồng bộ với Tailwind config.
- Product không tự định nghĩa breakpoint khác khi đã dùng Design System.
